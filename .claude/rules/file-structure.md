# ファイル構成(TypeScript/Node.jsコード)

- `.ts`ファイルは目安150行、200行を超えたら分割を検討する。
- 1ファイルが複数の責務を持ったら、行数に関わらず分割を検討する。
- 現状`tools/png2dds/`程度の小規模なビルドスクリプト群なので、`scripts/check-structure.ts`のような自動チェックの仕組みは導入していない(過剰設計を避ける)。コード量が増えて分割判断が難しくなってきたら、そのとき導入を検討する。
