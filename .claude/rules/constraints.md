# 制約(TypeScript/Node.jsコード)

- 理由のない新規依存追加をしない。数行で済む自前コードで済むなら、依存を増やさない。
- DIコンテナを使わない。
- デコレータを使わない。
- OOP(class/継承)は本人が明示的に指定しない限り使わない。
- Civ6側(XML/Lua)の固有事情・ハック(ModBuddyのスキーマ差異、`.dep`の機械的変換等)はこのファイルの対象外。`.claude/skills/`の各Skillを参照。
